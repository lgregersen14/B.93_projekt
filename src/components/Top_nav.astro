---

---
  <nav class="topnav">
    <a href="/" class="logo">
      <div class="blurred-bg logo-box"> <img src="Logo.png" alt="Logo" class="logo-img" /></div>
    </a>
    <div class="nav-links">
      <a href="#" class="nav-link"><span class="blurred-bg link-bg">KAMPDAGEN</span></a>
      <a href="#" class="nav-link"><span class="blurred-bg link-bg">MERCHANDISE</span></a>
      <a href="/historie" class="nav-link"><span class="blurred-bg link-bg">HISTORIE</span></a>
    </div>
  </nav>
<style>
 /* Reset default margin/padding */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  padding-top: 60px; /* prevent content from hiding behind the fixed nav */
}

.topnav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 20px;
  height: 60px;
  background: transparent; /* Keep transparent so blur shows through if needed */
  backdrop-filter: blur(4px); /* Optional: blur the background behind the nav */
}

/* Logo styles */
.logo {
  width: 32.8px;
  height: 42px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 8px;
  overflow: hidden;
}

.logo-box {
  width: 100%;
  height: 100%;
  position: relative;
  border-radius: 8px;
  overflow: hidden;
}

/* Navigation links */
.nav-links {
  display: flex;
  gap: 50px;
}

.nav-link {
  text-decoration: none;
 color: #00084b;  font-size: 16px;
}

.link-bg {
  display: inline-block;
  padding: 8px 16px;
  backdrop-filter: blur(8px);
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8px;
}

.blur-layer {
  position: absolute;
  inset: 0;
  backdrop-filter: blur(8px);
  background: rgba(255, 255, 255, 0.1);
  z-index: 0;
  border-radius: 8px;
}

.logo-img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  z-index: 1;
  border-radius: 8px;
}

</style>