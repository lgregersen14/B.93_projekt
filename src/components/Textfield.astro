---
---


    <div class="header-line">
      <h1 class="left1">Kærlighed.</h1>
      <h1 class="center1">Sammenhold.</h1>
      <h1 class="right1">Ægte.</h1>
    </div>
    <section class="row-container">
      <div class="left">
        <h1>Oplev Kampdagen</h1>
      </div>
      <div class="middle">
        <p>
          Velkommen til en kampdag på Østerbro Stadion! Her samles vi om
          fodbolden, fællesskabet og den gode stemning. Uanset om du er fast fan
          eller førstegangsbesøgende, er der altid plads til dig på tribunen.
        </p>
      </div>
      <div class="right">
        <a href="/kampdagen" class="svg-button">
          <!-- Replace with your SVG -->
          <img src="click_botton.svg" alt="My SVG Icon" width="40" height="40" />
        </a>
      </div>
    </section>

    <style>
        @font-face {
  font-family: "display-italic";
  src: url(B.93-Display-Italic.otf);
}
@font-face {
  font-family: "display";
  src: url(B.93-Display.otf);
}
@font-face {
  font-family: "body";
  src: url(B.93-Text.otf);
}
@font-face {
  font-family: "Morion";
  src: url(Morion-Regular.otf);
}



.header-line {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  justify-items: center;
  margin-bottom: 120px;
  color: #00084b;
  font-size: clamp(1rem, 1vw, 16px);
  font-family: "display-italic";
  padding-top: 65px;
}

.header-line h1 {
  margin: 0;
  font-weight: normal;
  white-space: nowrap;
}

/* Center word bold */
.header-line .center1 {
  font-weight: bold;
  position: relative;
}

/* Left and right pushed exactly 60px from center */
.header-line .left1 {
  justify-self: end;
  margin-right: 60px;
}

.header-line .right1 {
  justify-self: start;
  margin-left: 60px;
}

.row-container {
  display: flex;
  width: 100%;
  box-sizing: border-box;
  padding-bottom: 65px;
}

.left {
  width: 50%;
  padding: 1rem;
  display: flex;
  align-items: flex-start;
  position: relative;
  overflow: hidden;
  padding-left: 30px;
}

.left h1 {
  font-size: clamp(1.5rem, 3vw, 2rem);
  opacity: 0;
  position: relative;
  animation: fadeInText 0.8s ease forwards;
  animation-delay: 0.8s;
  font-family: "Morion";
  color: #00084b;
  margin-top: 0;
}

.left .mask {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: white;
  animation: maskReveal 0.8s ease forwards;
}

.middle {
  width: 30%;
  padding: 1rem;
  display: flex;
  align-items: flex-start;
  opacity: 0;
  animation: fadeIn 0.4s ease forwards;
  animation-delay: 1.2s;
}

.middle p {
  font-size: clamp(1rem, 2vw, 1rem);
  font-family: "body";
  color: #00084b;
  line-height: 1.4;
  margin-top: 0;
}

.right {
  width: 20%;
  padding: 1rem;
  display: flex;
  align-items: flex-start;
  opacity: 0;
  animation: fadeIn 0.4s ease forwards;
  animation-delay: 1.4s;
}

.svg-button {
  background: none;
  border: none;
  cursor: pointer;
  padding: 0 1rem;
  transition: transform 0.2s ease;
}

.svg-button:hover {
  transform: scale(1.1);
}

/* Animations */
@keyframes maskReveal {
  from {
    height: 100%;
  }
  to {
    height: 0;
  }
}

@keyframes fadeInText {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
    </style>
