---
---

    <div class="container">
      <div class="box">
        <h2>SENESTE NYT</h2>
        <div class="image-container">
          <img src="image_1.jfif" alt="Placeholder" />
          <p class="overlay-text">
            Dramatisk afslutning da b.93 Bookede plads i 1. division
          </p>
        </div>
        <p class="below-text">
          Med tre runder tilbage har B.93 sikret pladsen i NordicBet Ligaen også
          i 2025/26 sæsonen
        </p>
      </div>

      <div class="box">
        <h2>SENESTE NYT</h2>
        <div class="image-container">
          <img src="image_1.jfif" alt="Placeholder" />
          <p class="overlay-text">
            Dramatisk afslutning da b.93 Bookede plads i 1. division
          </p>
        </div>
        <p class="below-text">
          Med tre runder tilbage har B.93 sikret pladsen i NordicBet Ligaen også
          i 2025/26 sæsonen
        </p>
      </div>

      <div class="box">
        <h2>SENESTE NYT</h2>
        <div class="image-container">
          <img src="image_1.jfif" alt="Placeholder" />
          <p class="overlay-text">
            Dramatisk afslutning da b.93 Bookede plads i 1. division
          </p>
        </div>
        <p class="below-text">
          Med tre runder tilbage har B.93 sikret pladsen i NordicBet Ligaen også
          i 2025/26 sæsonen
        </p>
      </div>
    </div>

    <style>@font-face {
  font-family: "display-italic";
  src: url(fonts/B.93-Display-Italic.otf);
}
@font-face {
  font-family: "display";
  src: url(fonts/B.93-Display.otf);
}
@font-face {
  font-family: "body";
  src: url(fonts/B.93-Text.otf);
}
@font-face {
  font-family: "Morion";
  src: url(fonts/Morion-Regular.otf);
}
/* Global styles */
body {
  box-sizing: border-box;
}

/* Container to hold the three boxes */
.container {
  display: flex;
  justify-content: space-between;
  gap: 30px;
  padding: 0 30px;
}

/* Individual box */
.box {
  position: relative;
  flex: 1;
  display: flex;
  flex-direction: column;
}

/* Title above the image */
.box h2 {
  margin-bottom: 15px;
  text-align: left;
  font-family: "Morion";
  color: #111764;
}

/* Line above h2 for all boxes (real for first/last, invisible for middle) */
.box:first-child h2::before,
.box:last-child h2::before,
.box:nth-child(2) h2::before {
  content: "";
  display: block;
  height: 1px;
  width: 100%;
  margin-bottom: 17px;
}

/* Real black lines on left and right boxes */
.box:first-child h2::before,
.box:last-child h2::before {
  background-color: #111764;
}

/* Invisible line on middle box */
.box:nth-child(2) h2::before {
  background-color: transparent;
}

/* Image container with responsive height */
.image-container {
  position: relative;
  height: clamp(250px, 20vw, 400px);
  background: white;
  overflow: hidden;
}

/* Image itself */
.image-container img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 0.3s ease; /* smooth zoom */
}

/* Text over image, vertically centered, left with 10px padding */
.image-container .overlay-text {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  left: 10px;
  color: white;
  font-family: "display";
  font-size: 32px;
  max-width: 60%;
}

/* Text below the box */
.below-text {
  margin-top: 15px;
  text-align: left;
  font-family: "body";
  color: #111764;
  font-size: 13px;
  max-width: 65%;
}

.box:hover .image-container img {
  transform: scale(1.05); /* slightly zoom in */
} answer ok</style>